# FastCull performance — PRD 3.3 prefetch + LRU cache (2026-08-23)

Generated by `dotnet run -c Release --project Fastcull.Benchmarks`.
Run at 2026-08-23 21:54:02Z UTC.

This is the reference point for PRD 3.5's "a regression greater than 25% fails the
build" rule. Wiring that into an actual CI gate is future work — this file is the
baseline it would compare against, not the gate itself.

## Environment

| | |
| :--- | :--- |
| Machine | OFFICE |
| OS | Microsoft Windows NT 10.0.26200.0 |
| Logical processors | 12 |
| Scan workers (coreCount - 2) | 10 |
| Runtime | 9.0.19 |
| Build | Release |
| Server GC | False |
| Real corpus files | 101 |
| Synthetic corpus files | 2,000 (hard links over 101 distinct files) |

## Measured

| Metric | PRD budget | Measured | Status |
| :--- | :--- | ---: | :--- |
| Full scan, 2,000 files | < 8 s | 217.2 ms | PASS |
| Full scan, real corpus (control) | no PRD budget | 13.1 ms | - |
| First image, `all-RAW` (decode only, warm folder) | < 1.5 s | 74.6 ms | PASS |
| First image, `all-JPEG` (decode only, warm folder) | < 1.5 s | 13.2 ms | PASS |
| First image, `mixed` (decode only, warm folder) | < 1.5 s | 16.2 ms | PASS |
| First image, 2,000 files - **if it streamed** (PRD 1.2 behaviour) | < 1.5 s | 231.3 ms | PASS |
| First image, 2,000 files - **as the app sequences it today** | < 1.5 s | 231.4 ms | PASS |
| Next/prev cache miss, `all-RAW`, thumbnail tier (median) | < 250 ms | 14.1 ms | PASS |
| Next/prev cache miss, `all-RAW`, display tier (median) | < 250 ms | 22.6 ms | PASS |
| Next/prev cache miss, `all-JPEG`, thumbnail tier (median) | < 250 ms | 7.3 ms | PASS |
| Next/prev cache miss, `all-JPEG`, display tier (median) | < 250 ms | 8.1 ms | PASS |
| Next/prev cache miss, `mixed`, thumbnail tier (median) | < 250 ms | 13.8 ms | PASS |
| Next/prev cache miss, `mixed`, display tier (median) | < 250 ms | 23.7 ms | PASS |
| Rating keypress to new cull state (decision path only) | < 16 ms | 0.1 µs | PASS |
| Drain all 2,000 files' decodes (disposing bitmaps) | no PRD budget | 36.32 s | - |
| Drain all 2,000 files' decodes (retaining bitmaps) | no PRD budget | 36.59 s | - |
| Next/previous, cache hit | < 16 ms | 11.3 ms | PASS |
| Next/previous, cache hit — **decision only, heap quiesced** | < 16 ms | 10.1 µs | PASS |
| Peak working set, 2,000 files (transient decode only, bitmaps disposed) | < 4 GB | 667.56 MB | PASS |
| Peak working set, 2,000 files — **bitmaps retained, as the app does** | < 4 GB | 5.16 GB | **FAIL** |
| Peak working set, 2,000 files — **PRD 3.3 window + LRU eviction (the shipping path)** | < 4 GB | 3.26 GB | PASS |
| Peak working set with a zoom held mid-session | < 4 GB | 2.60 GB | PASS |

### Detail

- **Full scan, 2,000 files** — 2000 files (synthetic, hard-linked). 0.11 ms/file.
- **Full scan, real corpus (control)** — 101 genuinely distinct files. 0.13 ms/file - compare against the synthetic 0.11 ms/file to judge how much the hard-linked corpus's warm page cache flatters the row above.
- **First image, `all-RAW` (decode only, warm folder)** — DSC02667.ARW, thumbnail tier. Excludes the scan - see the two rows below.
- **First image, `all-JPEG` (decode only, warm folder)** — _MG_4211.jpg, thumbnail tier. Excludes the scan - see the two rows below.
- **First image, `mixed` (decode only, warm folder)** — DSC02667.ARW, thumbnail tier. Excludes the scan - see the two rows below.
- **First image, 2,000 files - if it streamed (PRD 1.2 behaviour)** — Folder-open to first decoded thumbnail, decoding the first photo the scanner yields without waiting for the scan to finish. This is the behaviour PRD 1.2 specifies. The app does NOT do this today.
- **First image, 2,000 files - as the app sequences it today** — MainViewModel.LoadAsync awaits the entire scan and sort before constructing the first FilmstripItemViewModel, which is what starts a decode. Excludes SessionStore registration, so the real app is at least this slow, not less. Difference vs. the streaming row above: 0 ms. A near-zero or negative difference does NOT vindicate the architecture - it means the scan is currently too fast (warm cache, 2,000 hard links) for the difference to show. The gap grows with scan cost, which is exactly what a cold 2,000-file NVMe folder has and this run does not.
- **Next/prev cache miss, `all-RAW`, thumbnail tier (median)** — n=24, p95 17.1 ms, max 24.7 ms.
- **Next/prev cache miss, `all-RAW`, display tier (median)** — n=24, p95 27.3 ms, max 29.0 ms.
- **Next/prev cache miss, `all-JPEG`, thumbnail tier (median)** — n=4, p95 7.6 ms, max 7.6 ms.
- **Next/prev cache miss, `all-JPEG`, display tier (median)** — n=4, p95 9.4 ms, max 9.4 ms.
- **Next/prev cache miss, `mixed`, thumbnail tier (median)** — n=24, p95 17.3 ms, max 17.5 ms.
- **Next/prev cache miss, `mixed`, display tier (median)** — n=24, p95 26.6 ms, max 29.5 ms.
- **Rating keypress to new cull state (decision path only)** — n=100,000, final state Unflagged/0. InputRouter.Resolve + CullState transition only - the PropertyChanged raise lives in the WinUI project and the compositor repaint is not the app's to time.
- **Drain all 2,000 files' decodes (disposing bitmaps)** — Every item's thumbnail and display decode, launched unthrottled and drained.
- **Drain all 2,000 files' decodes (retaining bitmaps)** — Every item's thumbnail and display decode, launched unthrottled and drained.
- **Next/previous, cache hit** — n=1,999 hits, 0 misses (100.0% hit rate). p95 14.5 ms, max 87.3 ms. A hit is counted only where the incoming photo's display-tier bitmap was ALREADY decoded when the cursor arrived - a step that had to decode is recorded as a miss, so this row cannot quietly report a decode as a cache hit. **This is measured while the decode pipeline is saturated** (the walk settles the prefetch at every step), so it includes GC pauses the decode work inflicts on the navigating thread - see the quiesced row below, which isolates the decision itself. Excludes the SoftwareBitmapSource marshal (~30 ms, measured earlier in this project).
- **Next/previous, cache hit — decision only, heap quiesced** — n=500, p95 10 µs, max 16 µs. Same navigation work - stage window, pin pass, and the coordinator's O(n) sweep over all 2,000 items - with nothing decoding and the heap collected first. The gap between this and the row above is not the cache: it is what GC pressure from the decode pipeline costs the navigating thread while it is running flat out.
- **Peak working set, 2,000 files (transient decode only, bitmaps disposed)** — 4,000 decode jobs launched over 2,000 files, unthrottled, mirroring FilmstripItemViewModel's constructor. Launch loop 36.2 s, all jobs drained at 36.3 s. Baseline working set 84 MB. Peak managed heap 588 MB. Each bitmap disposed as soon as it decoded, which the app does NOT do - see the retained row.
- **Peak working set, 2,000 files — bitmaps retained, as the app does** — 4,000 decode jobs launched over 2,000 files, unthrottled, mirroring FilmstripItemViewModel's constructor. Launch loop 36.5 s, all jobs drained at 36.6 s. Baseline working set 607 MB. Peak managed heap 590 MB. 4,000 decoded bitmaps held live at peak - the app never releases them.
- **Peak working set, 2,000 files — PRD 3.3 window + LRU eviction (the shipping path)** — Cursor stepped through all 2,000 photos, settling the prefetch at every step so every photo genuinely decodes. Nine stage slots (FilmstripWindow.MaxSlots - the worst case), +5/-2 window, DecodeGate at 6 workers, 3 GB LRU ceiling. 745 evictions over the run. Peak 2,000 items resident at once, 3074 MB of tracked decoded pixels. Baseline working set 382 MB. Peak managed heap 228 MB. Walk took 81.1 s.
- **Peak working set with a zoom held mid-session** — Sampled at photo 1,000 of 2,000, with the cache already at steady state - not on an empty heap. Zoom tier decoded at a 3440px long edge in 133 ms, holding 30 MB of BGRA8. Released immediately after, as the app does on Space/Escape or a photo change. Only one zoom is ever alive, so this does not accumulate.

## Not measurable yet

These are PRD 3.5 budgets with no number, because the feature they measure does not
exist. None of them was approximated by timing something adjacent.

| Metric | PRD budget | Why there is no number |
| :--- | :--- | :--- |
| Enter zoom, Tier A cached | < 50 ms | Zoom mode does not exist (PRD 1.7, scheduled v0.2). Nothing to enter, nothing to time. |
| Enter zoom, Tier B | < 400 ms CPU / < 100 ms GPU | Zoom mode does not exist (v0.2), and there is additionally no debayer implementation at all for the Tier B path to use - see PRD 3.4's open gap. |
| Pan at 1:1 | sustained 60 fps | Zoom mode does not exist (v0.2). A frame-rate measurement also needs a compositor and a real window, which a headless harness does not have. |

## Corpus

- Fixture set `all-RAW`: 97 real files.
- Fixture set `all-JPEG`: 4 real files.
- Fixture set `mixed`: 101 real files.

## How to read this

- **Real vs. synthetic file counts.** The real corpus is 101 files. The two budgets
  PRD 3.5 defines at "2,000 files" are measured against a synthetic corpus of 2,000
  NTFS hard links cycling over those 101 files — **2,000 real photos were not used**.
  Hard links are genuine directory entries over the real bytes, so enumeration,
  `FileInfo.Length` and header parsing all do real work, but 2,000 entries share 101
  extents, so the OS page cache is warmer than it would be for 2,000 distinct files.
  That biases scan and decode timings **optimistically**. The "real corpus (control)"
  row exists to bound that bias: compare its ms/file against the synthetic one.
- **Every timing here is warm-cache.** The harness scans the corpus once to build
  its fixture sets before it measures anything, so the OS page cache is already hot
  by the time the scan is timed. A genuinely cold first-open of a 2,000-file folder
  off NVMe is **not** measured by this run and will be slower — possibly much
  slower. Read the scan and first-image rows as a floor, not as the number a user
  sees when they open a card for the first time.
- **"First image" is a proxy.** It measures folder-open to the first decoded
  thumbnail — pixels in hand, not pixels lit. It stops before the XAML
  `SoftwareBitmapSource` marshal, previously measured at roughly 30 ms, negligible
  against a 1.5 s budget.
- **The rating row is decision latency**, not end-to-end. `PropertyChanged` is raised
  in `FilmstripItemViewModel`, which lives in the WinUI project and cannot be
  constructed headlessly; the compositor repaint after it is GPU-scheduled and not
  the app's to time.
- **Two peak-working-set rows measure two different architectures, on purpose.** The
  "bitmaps retained" row reproduces the pre-PRD-3.3 code: every constructed item started
  both decodes immediately and nothing ever released one. The "window + LRU eviction"
  row reproduces what ships now. The first is kept deliberately — it is the number the
  second has to beat, and deleting it would hide the comparison.
- **The memory rows hold `SoftwareBitmap`, the app holds `SoftwareBitmapSource`.** A
  `SoftwareBitmapSource` additionally copies pixels into a XAML composition surface,
  which may live in GPU memory and is not this process's working set to sample. Read
  every peak here as **system RAM only** — the PRD 3.5 budget's own unit — and note that
  PRD 3.3's VRAM ceiling is separate, v0.4 scope, and unbuilt.
- **Eviction disposes nothing.** Both the app and this harness drop the last managed
  reference and let the GC reclaim, because disposing a bitmap XAML may still be copying
  fail-fasts the process with `0xC000027B`. Peak working set therefore depends on when
  the GC runs, not only on when the app lets go. That is real app behaviour, not a
  harness artefact.
- **The cache-hit row counts only genuine hits.** A step where the incoming photo still
  had to be decoded is recorded as a miss and excluded from the timing, so the number
  cannot be a decode wearing a cache-hit label. Its hit rate is in the row's detail.
- **No BenchmarkDotNet.** PRD 3.5 wants a regression gate that runs routinely, not a
  statistically rigorous micro-benchmark suite with warm-up iterations taking minutes
  per run. A `Stopwatch` harness running the real pipeline against the PRD's absolute
  budgets fits PRD 5's architecture (`Benchmarks/PerfHarness.cs`) and this project's
  no-new-packages default. The tradeoff is real and worth naming: single-run timings
  are noisier than a proper harness would produce, so treat a change under about 25%
  as noise — which is, conveniently, exactly the threshold PRD 3.5 sets.
